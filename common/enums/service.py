"""Enum for storing authentication service."""

from enum import StrEnum


class Service(StrEnum):
    """Enum for storing authentication service."""

    FRONTIER = "Frontier"
    STEAM = "Steam"
    EPIC = "Epic"
