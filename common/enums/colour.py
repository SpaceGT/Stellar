"""Enum for storing colours."""

from enum import StrEnum


class Colour(StrEnum):
    """Enum for storing colours."""

    BLACK = "Black"
    BLUE = "Blue"
    CYAN = "Cyan"
    GREEN = "Green"
    YELLOW = "Yellow"
    RED = "Red"
    PURPLE = "Purple"
